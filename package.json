{
  "name": "website",
  "scripts": {
    "build:event": "parcel build --no-cache --no-source-maps --public-url /assets/ --dist-dir=assets/ _javascript/event.js",
    "build:stripe": "parcel build --no-cache  --no-source-maps --public-url /assets/ --dist-dir=assets/ _javascript/stripe.js"
  },
  "devDependencies": {
    "leaflet": "^1.7.1",
    "parcel": "^2.3.2"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead"
}
