{
  "name": "website",
  "scripts": {
    "build:event": "parcel build --no-cache --no-source-maps --public-url /assets/ --out-dir=assets/ --out-file=event.js _javascript/event.js",
    "build:stripe": "parcel build --no-cache  --no-source-maps --public-url /assets/ --out-dir=assets/ --out-file=stripe.js _javascript/stripe.js"
  },
  "devDependencies": {
    "leaflet": "^1.6.0",
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {},
  "browserslist": [
    "last 3 chrome version",
    "last 3 firefox version",
    "last 3 safari version"
  ]
}